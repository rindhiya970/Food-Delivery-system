server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_delivery
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379

logging:
  level:
    root: INFO

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: fooddelivery.com.food_delivery.entity
