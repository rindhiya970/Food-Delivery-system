<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fooddelivery.com.food_delivery.mapper.UserMapper">

    <select id="findByEmail" resultType="fooddelivery.com.food_delivery.entity.User">
        SELECT id, email, password, role
        FROM users
        WHERE email = #{email}
    </select>

    
 
    <insert id="insertUser" parameterType="fooddelivery.com.food_delivery.entity.User">
    INSERT INTO users (username, email, password, role)
    VALUES (#{username}, #{email}, #{password}, #{role})
</insert>



</mapper>

